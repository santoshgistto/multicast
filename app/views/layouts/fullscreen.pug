doctype html
html(lang='en')
  head
    title Multicast | #{channel.name}
    include ../include/head.pug
    link(rel='stylesheet', href='/css/channel.css')

  body.fullscreen(class=rotation)
    include ../include/error.pug
    include ../include/toast.pug
    if channel.list != null && channel.list.length
      iframe(src=channel.list[0].url, frameborder=0, framespacing=0)
    
    include ../include/rotation.pug
    script(type='text/javascript', src='/js/jquery.min.js')
    script(type='text/javascript', src='/socket.io/socket.io.js')
    if casting
      script(type='text/javascript', src='//www.gstatic.com/cast/sdk/libs/receiver/2.0.0/cast_receiver.js')
    script(type='text/javascript', src='/js/app.js')
    script(type='text/javascript', src='/js/client.js')
